/** @jsxImportSource @emotion/react */
import { css, jsx } from '@emotion/react';
const NoMatch = () => {
  return (
    <div css={css`
      color: white;
    `}>
      Sorry this site is currently under construction and the page you are looking for doesn't exist yet. 
    </div>
  )
}

export default NoMatch